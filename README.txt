# Avalanche_mapping

I needed a easy way to map avalanches into polygons.

Steps:
Use the google earth engine editor to map polygons manually
Export to geojson file
Run `geojson_to_geotiff.sh` to get a geotiff file

